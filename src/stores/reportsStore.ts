// Deprecated. Notifications now fetch real data from the Synth /api/reports endpoint
// directly in src/app/app/notifications/page.tsx. This module is no longer imported
// anywhere in the project and can be safely deleted from the repo.

export {};
