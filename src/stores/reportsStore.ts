// Deprecated. Notifications and the diagnostic detail page now fetch real data
// directly from the Synth API (/api/reports and /api/reports/<id>). This module
// is no longer imported anywhere in the project and can be safely deleted from
// the repo whenever convenient.

export {};
