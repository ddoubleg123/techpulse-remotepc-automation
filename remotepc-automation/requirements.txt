# Core dependencies (no GUI required)
redis>=5.0.1
rq>=1.16.1
requests>=2.31.0
python-dotenv>=1.0.1
PyPDF2>=3.0.1
pandas>=2.2.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Headless versions for server
pillow>=10.2.0
opencv-python-headless>=4.9.0.80

# GUI automation (only loaded by worker when needed)
# These will fail on headless server but that's OK - worker handles it
pyautogui>=0.9.54; platform_system != "Linux"
